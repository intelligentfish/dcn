syntax = "proto3";
package proto;
// message
message Message {
  int32 main = 1; // main type
  int32 sub = 2;  // sub type
  bytes pub = 3;  // public key
  bytes sig = 4;  // sign
  bytes body = 5; // body
}